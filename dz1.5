#include <iostream>
#include <string>


using namespace std;

int main()
{
	bool VkomnateSvetlo, NaUliceSvetlo, Lampa, Shtori;
	cout << "Введите значения(0 или 1(0 - ложь, 1 - истина)) для переменных: " << endl;
	cout << "Lampa = ";
	cin >> Lampa;
	cout << "NaUliceSvetlo = ";
	cin >> NaUliceSvetlo;
	cout << "Shtori = ";
	cin >> Shtori;
	if (((NaUliceSvetlo == true && Shtori == true) || Lampa) == true)
		cout << "В комнате светло";
	else
		cout << "В комнате темно";
    
}
