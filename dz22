#include <iostream>
#include <string>
#include <cmath>

using namespace std;

int main()
{
	float w, x, a;
	cout << "x = ";
	cin >> x;
	cout << "a = ";
	cin >> a;
	if (abs(x) < 1){
		w = a * log(abs(x));
	}else
		w = sqrt(a - x * x);
	cout << "w = " << w;
}
