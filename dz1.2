  
#include <iostream>
#include <string>

using namespace std;

int main()
{
    int num1, num2, sum, raz, p;
    float d;
	cout << "num1 = ";
    cin >> num1;
	cout << "num2 = ";
    cin >> num2;
    sum = num1 + num2;
    raz = abs(num1 - num2);
    p = num1 * num2;
    d = num2 / num1;
    cout << "sum = " << sum << " raz = " << raz << " p = " << p <<" d = " << d;
    
}
